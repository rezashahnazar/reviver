We have a Next.js project for demoing a package we developed named reviver included within the project structure at packages/reviver.
"gpt-4o-mini" is the model we are using for the reviver package. The name is correct and don't consider it as a typo.